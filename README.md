# Dagger-2 Custom-Qualifiers

Problem with @Named Annotation is if there is spelling mistake in @Named annotation (e.g @Named("message") )
then it will throw error: [Dagger/MissingBinding] so we use custom Annotation

